// Demonstrates booleans in C
#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool isRaining = true;
    bool isSunny = false;
    printf("Is it raining? %s\n", isRaining ? "Yes" : "No");
    printf("Is it sunny? %s\n", isSunny ? "Yes" : "No");
    return 0;
}
